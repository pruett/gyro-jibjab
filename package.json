{
  "name": "gyro-holiday-2016",
  "version": "1.0.0",
  "description": "jibjab-style greeting card",
  "main": "index.js",
  "scripts": {
    "dist": "rm -rf dist && mkdir dist && cp -R src/ dist && gulp && surge -p dist -d gyro-deckthehalls.surge.sh"
  },
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "autoprefixer": "^6.5.4",
    "gulp": "^3.9.1",
    "gulp-autoprefixer": "^3.1.1"
  }
}
